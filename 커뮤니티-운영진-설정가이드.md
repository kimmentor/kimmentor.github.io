# 커뮤니티 · 운영진(관리자) 설정 가이드 — 구글 로그인

커뮤니티가 **Q&A · 자유게시판 · 운세상담** 3개 분류로 확장됐고, 운세상담에는 **비밀글**(운영진만 열람) 기능이 붙었습니다.
운영진 로그인은 **구글 계정 로그인(팝업)** 방식입니다. 아래 **세 가지만** 설정하면 실제로 동작합니다. (약 10분)

---

## ① 구글 로그인 켜기 (Firebase Authentication)

1. https://console.firebase.google.com 접속 → 프로젝트 **kimmentor-6d212** 선택
2. 왼쪽 메뉴 **빌드 → Authentication → 시작하기**
3. **Sign-in method(로그인 방법)** 탭 → **Google** 선택 → **사용 설정** 켜기
   - 프로젝트 지원 이메일을 고르라고 하면 본인 이메일을 선택하고 **저장**
4. 같은 화면 아래 **승인된 도메인(Authorized domains)** 에 블로그 주소를 추가
   - GitHub Pages라면 보통 `본인아이디.github.io` (예: `kimmentor.github.io`)
   - `localhost` 와 `*.firebaseapp.com` 은 이미 등록돼 있어요

> 승인된 도메인에 블로그 주소가 없으면 구글 로그인 팝업이 뜨지 않으니 꼭 추가하세요.

---

## ② 운영진 이메일 지정 (이미 넣어둠 — 확인만)

"아무 구글 계정"이나 운영진이 되면 안 되므로, **지정한 이메일만** 운영진 권한을 갖습니다.
현재 두 곳에 **`dongsik2026@gmail.com`** 으로 넣어두었어요. 이 계정으로 로그인하면 운영진이 됩니다.

- `firebase-config.js` 의 `window.ADMIN_EMAIL`
- `firestore.rules` 의 `request.auth.token.email == "..."`

**다른 구글 계정으로 운영**하려면 위 **두 곳의 이메일을 똑같이** 바꿔주세요.

---

## ③ 보안 규칙 게시 (Firestore 규칙)

1. 왼쪽 메뉴 **빌드 → Firestore Database → 규칙(Rules)** 탭
2. 기존 내용을 지우고 함께 드린 **`firestore.rules`** 내용을 그대로 붙여넣기
3. **게시(Publish)**

이 규칙이 하는 일:

- `comments`, `qna_questions`, `qna_answers` — 누구나 읽고 쓰는 공개 영역(기존 유지)
- `qna_secret`(운세상담 비밀글) — **등록은 누구나** 가능하지만, **열람·수정·삭제는 지정한 운영진 구글 계정만** 가능
  → 방문자는 물론, 데이터베이스를 직접 열어봐도 그 계정 로그인 없이는 비밀 상담 내용을 볼 수 없습니다. (진짜 보안은 이 규칙이 지킵니다)

---

## 어떻게 동작하나요

- **로그인**: 커뮤니티 오른쪽 위 **“🔒 운영진 로그인 (Google)”** → 구글 팝업으로 로그인.
  지정한 운영진 이메일이면 **🛡️ 운영진 모드**가 되고, 다른 계정이면 자동 로그아웃되며 안내가 뜹니다.
- **운영진 모드**: 모든 글·답변을 비밀번호 없이 **조회·수정·삭제**하고, **비밀 상담 글도 열람**됩니다. 로그아웃하면 일반 이용자와 동일해집니다.
- **비밀 상담**: 글쓰기에서 **운세상담** 선택 → **“🔒 비밀글로 등록”** 체크 → 등록. 목록·검색에 노출되지 않고 운영진 로그인 시에만 보입니다. (작성자 본인도 다시 열람 불가 — 운영진 전용 설계)

---

## 배포

GitHub Pages를 쓰신다면 아래 파일을 다시 업로드하세요.

- `qna.html`, `qna-view.html`, `qna.js` (커뮤니티)
- `firebase-config.js` (운영진 이메일 포함)

`firestore.rules` 는 업로드가 아니라 위 ③처럼 **Firebase 콘솔에 붙여넣어 게시**하는 파일입니다.

궁금한 점이 있으면 언제든 다시 불러주세요!
